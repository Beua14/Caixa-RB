\
// main.dart - Caixa RB (starter project regenerated)
// NOTE: This is a starter template. Replace with full code as needed.

import 'package:flutter/material.dart';

void main() {
  runApp(const CaixaRBApp());
}

class CaixaRBApp extends StatelessWidget {
  const CaixaRBApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Caixa RB',
      theme: ThemeData(
        scaffoldBackgroundColor: Color(0xFFFAFAFA),
        primarySwatch: Colors.blue,
      ),
      home: const Scaffold(
        body: Center(child: Text('Caixa RB - App template (substitua pelo código completo)')),
      ),
    );
  }
}
